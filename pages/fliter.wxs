var filters = {
  toFix: function (value) {
    return Math.floor(value*100)/100 //此处2为保留两位小数
  },
  parseInt: function (value) {
    return parseInt(value)
  }
}

module.exports = {
  toFix: filters.toFix,
  parse: filters.parseInt
}